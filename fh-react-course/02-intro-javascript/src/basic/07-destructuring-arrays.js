// Desestructuración de arreglos
// asignación desestructurante de arreglos

const character = ['Goku', 'Vegueta', 'Trunks'];

console.log(character);
console.log(character[0]);

// =>

const [p1, , p3] = character; 

console.log(p1, p3);